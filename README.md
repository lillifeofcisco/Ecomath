# Ecomath

AI study assistant for University of Ghana Elements of Mathematics in Economics.

This version uses OpenAI.

## Local setup

1. `npm install`
2. copy `.env.example` to `.env`
3. add your `OPENAI_API_KEY` to `.env`
4. run `npm start`
5. open `http://localhost:3000`

Do not commit your `.env` file.

## Local routes

### 1. AI tutor
- `http://localhost:3000`

### 2. Economics Visual Lab
- `http://localhost:3000/econ-visual-lab/`

## GitHub Pages note

The static visual lab is stored in `docs/`.

Expected Pages URL after GitHub Pages is enabled:
- `https://lillifeofcisco.github.io/Ecomath/`

Recommended Pages setting:
- branch `main`
- folder `/docs`
